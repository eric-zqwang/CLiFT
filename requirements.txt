dacite
einops
hydra-core
imageio
imageio-ffmpeg
jaxtyping
# lightning >= 2.6 refuses to load our checkpoints under torch >= 2.6
# (weights_only torch.load rejects the pickled omegaconf config).
lightning==2.5.2
lpips
moviepy
scikit-video
tqdm
wandb
scikit-image
scikit-learn
numpy==1.26.3
scipy==1.11.4