name: dl3dv
data_dir: ../Dataset/DL3DV-ALL-480P-images/
image_size: [256, 448]
batch_size: 4
val_batch_size: 4
num_workers: 8
overfit: -1
normalize_camera: true
min_context_views: 4
max_context_views: 6
# Precomputed K-means assignments (see save_kmeans.py). Selects the
# second-stage (condenser) training dataset when set.
kmeans_dir: null